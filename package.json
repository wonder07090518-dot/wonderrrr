{
  "name": "wonder-adlab-official-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build:upload-client": "esbuild reference-upload-client.entry.js --bundle --format=esm --platform=browser --minify --outfile=reference-upload-client.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@vercel/blob": "2.8.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
